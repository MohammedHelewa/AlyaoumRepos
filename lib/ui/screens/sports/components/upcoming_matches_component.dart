import 'package:alyaoum/common/app_libraries.dart';

class UpcomingMatchesComponent extends StatelessWidget {
  const UpcomingMatchesComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}